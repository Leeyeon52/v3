name: t0703
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.8.1 <4.0.0' # 사용자 요청에 따라 SDK 버전 조정

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8 # 최신 안정 버전으로 조정
  go_router: ^14.0.2 # 최신 안정 버전으로 조정
  provider: ^6.1.2 # MVVM ViewModel 바인딩용 Provider
  http: ^1.2.1 # HTTP 패키지 (최신 안정 버전으로 조정)
  shared_preferences: ^2.2.3 # ✅ 추가됨: 로컬 데이터 저장을 위해 필요
  intl: ^0.19.0 # ✅ 추가됨: 날짜/시간 포맷팅 및 다국어 지원을 위해 필요

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1 # 안정 린트

flutter:
  uses-material-design: true # 올바른 위치

